---
"@act-sdk/adapters": minor
---

Add Express and Hono HTTP adapters (`@act-sdk/adapters/express`, `@act-sdk/adapters/hono`) with optional auth and Streamable HTTP MCP, sharing core logic with the Next.js adapter.
